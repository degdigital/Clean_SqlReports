tables:
  cleansql_report:
    - report_id: 1
      sql_query: 'SELECT * FROM dataflow_profile'
      title: Dataflow Profile
      output_type: PlainTable
      chart_config:
      grid_config:
      cron_expr: '* * * * *'
      cron_job_code: dataflow_profile
      cron_export_type: csv